[pytest]
testpaths = tests/unit/python
markers =
    integration: opt-in Python integration tests; run explicitly, not by default
    e2e: opt-in Python end-to-end tests; run explicitly and rarely, not by default
